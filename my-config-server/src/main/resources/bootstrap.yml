server:
  port: 8081

spring:
  cloud:
    config:
      #name: configserver
      server:
        git:
          uri: https://github.com/EAAR85/Java-Config-Server.git
          skip-ssl-validation: true
          #search-paths: config-data
          searchPath: config-data
          username: EAAR85
          password: <123QWE123qwe>
          default-label: master
  security:
    user:
      name: root
      password: s3cr3t