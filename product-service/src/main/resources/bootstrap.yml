server:
  port: 9008

spring:
  application:
    name: product-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
      username: root
      password: s3cr3t

properties:
  list:
    - value 1
    - value 2
    - value 3
  type: type1